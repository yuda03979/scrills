# Examples

The repository ships a live project layer under `examples/.scrills/` — not a fixture:
`cd examples && scrills list` finds it, and a scrill is meant to be read before it is run. What
ships today is harness wiring.

--8<-- "examples/README.md:3"
