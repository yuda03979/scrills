# Examples

Four working scrills ship with the repository, under `examples/.scrills/`. It's a live project
layer, not a fixture — `cd examples && scrills list` finds them, and they are meant to be read
before they are run.

--8<-- "examples/README.md:3"
